# 8.1: AttendanceOverviewWidget.php Syntax Fix

**Story ID:** 8.1
**Epic:** 8 — PHPStan Bootstrap Fix
**Slug:** attendance-overview-widget-syntax-fix
**Status:** backlog

> Owned by planning until `ready-for-dev`. Sized to one dev-day (~2-8h). No story points.

## Story

As a **CI/CD pipeline**,
I want **PHPStan to bootstrap successfully**,
so that **static analysis can run and detect code quality issues**.

## Acceptance Criteria

1. PHPStan bootstrap completes without syntax errors
2. `AttendanceOverviewWidget.php` has valid PHP syntax (passes `php -l`)
3. `getFormSchema()` method returns proper array structure compatible with Filament v5
4. All Text entries use `->content(fn () => $this->renderMethod($data))` pattern

## Tasks / Subtasks

- [ ] Analyze the invalid syntax in `AttendanceOverviewWidget.php` lines 63-87 (AC: #1)
  - [ ] Identify the incorrect pattern `Text::make(...) : View => view(...)`
  - [ ] Compare with working widget `TeamPresenceWidget.php` (AC: #1)
- [ ] Replace invalid syntax with correct Filament v5 pattern (AC: #2, #3, #4)
  - [ ] Add private render methods for each tab content
  - [ ] Use `Text::make()->content(fn () => $this->renderMethod($data))` pattern
- [ ] Validate syntax with `php -l` (AC: #2)
- [ ] Test PHPStan bootstrap (AC: #1)

## Dev Notes

- **Filament v5 pattern for text content**: Use `Text::make('name')->content(fn () => $this->renderMethod($data))` where `renderMethod` returns a string [Source: TeamPresenceWidget.php:55-56]
- **Working reference**: `Modules/Employee/app/Filament/Widgets/TeamPresenceWidget.php` lines 55-56 and 154-191 show correct implementation
- **Invalid syntax to remove**: Lines 63-67, 73-77, 83-87 have `Text::make('...') : View => view(...)` which is invalid PHP
- **Widget class**: Extends `XotBaseSchemaWidget` which uses Filament Schemas v5 [Inference]

## Testing

- **Approach:** Syntax validation + bootstrap test
- **Key scenarios:** PHPStan bootstrap must complete; `php -l` must pass
- **Per-AC verification:** AC #1 → Run `phpstan analyse`; AC #2 → Run `php -l`; AC #3 → Review method structure; AC #4 → Verify pattern matches TeamPresenceWidget
- **Fixtures / mocks:** None required for syntax fix
- **Edge cases to cover:** None - this is a straightforward syntax correction

## Dependency Maps

- **Blocked by:** None
- **Blocks:** PHPStan analysis on all Modules
- **External:** Filament v5.7.8

## Owned File/Module Scope

- `Modules/Employee/app/Filament/Widgets/AttendanceOverviewWidget.php`

## Learnings from Previous Stories

- Pattern for Filament v5 Schema Widget content: `Text::make()->content(fn () => $this->renderMethod())` (from TeamPresenceWidget analysis)

## Dev Agent Record

_(empty — to be completed by the external dev tool)_
